<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>500 - Internal Server Error</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f8f9fa;
      color: #333;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .error-container {
      text-align: center;
      max-width: 600px;
      padding: 2rem;
    }
    .plug-icon {
      font-size: 5rem;
      color: #ffc107;
    }
    .error-code {
      font-size: 8rem;
      font-weight: 900;
      color: #0d6efd;
      line-height: 1;
    }
    .error-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: #212529;
      margin: 1rem 0;
    }
    .error-message {
      font-size: 1.3rem;
      color: #6c757d;
      margin-bottom: 2rem;
    }
    .btn-home {
      background: linear-gradient(135deg, #0d6efd, #0a58ca);
      border: none;
      padding: 0.75rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
    }
    @media (max-width: 576px) {
      .error-code {
        font-size: 5.5rem;
      }
      .error-title {
        font-size: 2rem;
      }
      .error-message {
        font-size: 1.1rem;
      }
      .plug-icon {
        font-size: 3.5rem;
      }
    }
  </style>
</head>
<body>

  <div class="error-container">
    <!-- Plug Icons + 500 -->
    <div class="d-flex align-items-center justify-content-center gap-4 mb-4 flex-wrap">
      <i class="bi bi-plug plug-icon"></i>
      <h1 class="error-code m-0"><%=statusCode %></h1>
      <i class="bi bi-plug-fill plug-icon"></i>
    </div>

    <!-- Title -->
    <h2 class="error-title">Internal Server Error! </h2>

    <!-- Message -->
    <p class="error-message">
      We're experiencing an internal server problem in accessing <%=url %>.<br>
      Please try again later.
    </p>

    <!-- Action Button -->
    <a href="/" class="btn btn-primary btn-home text-white shadow-lg">
      <i class="bi bi-house-door me-2"></i>
      Back to Home
    </a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>